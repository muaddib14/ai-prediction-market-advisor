import React from 'react'
import ReactDOM from 'react-dom/client'
import KalshorbPage from './KalshorbPage'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KalshorbPage />
  </React.StrictMode>,
)